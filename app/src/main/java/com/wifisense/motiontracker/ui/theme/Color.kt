package com.wifisense.motiontracker.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Palette ──────────────────────────────────────────────────────────
val NavyDeep        = Color(0xFF070B1A)
val NavySurface     = Color(0xFF0D1224)
val NavyCard        = Color(0xFF121830)
val NavyElevated    = Color(0xFF1A2240)

// ─── Accent / Brand ───────────────────────────────────────────────────────────
val CyanPrimary     = Color(0xFF00E5FF)
val CyanDim         = Color(0xFF00B8CC)
val CyanGlow        = Color(0x3300E5FF)
val PurpleAccent    = Color(0xFF7C4DFF)
val PurpleGlow      = Color(0x337C4DFF)

// ─── Motion State Colors ───────────────────────────────────────────────────────
val MotionStationary  = Color(0xFF00E5FF)    // Cyan — calm
val MotionMinor       = Color(0xFF69F0AE)    // Green — subtle
val MotionModerate    = Color(0xFFFFB300)    // Amber — active
val MotionHeavy       = Color(0xFFFF5252)    // Red — vigorous
val MotionCalibrating = Color(0xFF9E9E9E)    // Grey — calibrating

// ─── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary     = Color(0xFFEEF2FF)
val TextSecondary   = Color(0xFF8B9CC0)
val TextDisabled    = Color(0xFF4A5470)

// ─── Status / Semantic ────────────────────────────────────────────────────────
val Success         = Color(0xFF00E676)
val Warning         = Color(0xFFFFB300)
val Error           = Color(0xFFFF5252)

// ─── Gradients (used as brush stops) ─────────────────────────────────────────
val GradientStart   = Color(0xFF0D1224)
val GradientEnd     = Color(0xFF070B1A)
val GlowCyan        = Color(0x5500E5FF)
